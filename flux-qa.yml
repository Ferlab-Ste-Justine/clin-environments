apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: qa
  namespace: flux-clin-qa
spec:
  serviceAccountName: root-repo
  interval: 1m
  path: "./qa/fluxcd"
  sourceRef:
    kind: GitRepository
    name: root-repo
  prune: true