apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: terraform
spec:
  serviceAccountName: root-repo
  interval: 1m
  path: "./qa/apps/terraform"
  sourceRef:
    kind: GitRepository
    name: root-repo
  prune: true