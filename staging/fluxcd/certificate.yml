apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: certificate
spec:
  serviceAccountName: root-repo
  interval: 1m
  path: "./staging/apps/certificate"
  sourceRef:
    kind: GitRepository
    name: root-repo
  prune: true